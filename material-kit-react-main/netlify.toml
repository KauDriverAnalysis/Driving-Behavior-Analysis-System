[build]
  command = "NODE_OPTIONS='--max_old_space_size=4096' pnpm install --no-frozen-lockfile --strict-peer-dependencies=false --force && pnpm build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200